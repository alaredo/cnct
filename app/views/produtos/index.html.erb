<h1>Produtos</h1>

<style>
    table {
        text-align: left;
    }
</style>

<table>
  <thead>
    <tr>
      <th class="cab">Título</th>
      <th class="cab">Valor</th>
      <th class="cab">Saldo</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @produtos.each do |produto| %>
      <tr>
        <td><div style="width: 380px;"><%= produto.title %></div></td>
        <td><div style="width: 80px;"><%= produto.valor %></div></td>
        <td><div style="width: 80px;"><%= produto.saldo %></div></td>
        <td><%= link_to 'Show', produto %></td>
        <td><%= link_to 'Edit', edit_produto_path(produto) %></td>
        <td><%= link_to 'Destroy', produto, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Novo Produto', new_produto_path %>
