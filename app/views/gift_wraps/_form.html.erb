<%= form_for(@gift_wrap) do |f| %>
  <% if @gift_wrap.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@gift_wrap.errors.count, "error") %> prohibited this gift_wrap from being saved:</h2>

      <ul>
      <% @gift_wrap.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :available %><br>
    <%= f.check_box :available %>
  </div>
  <div class="field">
    <%= f.label :value %><br>
    <%= f.text_field :value %>
  </div>
  <div class="field">
    <%= f.label :message_support %><br>
    <%= f.check_box :message_support %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
