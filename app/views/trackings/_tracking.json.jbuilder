json.extract! tracking, :id, :controlPoint, :description, :occurredAt, :url, :number, :sellerDeliveryId, :cte, :carrier_id, :invoice_id, :created_at, :updated_at
json.url tracking_url(tracking, format: :json)
